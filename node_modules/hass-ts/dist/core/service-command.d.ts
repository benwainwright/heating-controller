import { Client } from "./client";
export interface ServiceCommandMap {
    "climate.set_hvac_mode": {
        entity_id?: string;
        hvac_mode: string;
    };
    "climate.set_preset_mode": {
        entity_id?: string;
        preset_mode: string;
    };
    "climate.set_aux_heat": {
        entity_id?: string;
        aux_heat: string;
    };
    "climate.set_temperature": {
        entity_id?: string;
        temperature?: number;
        target_temp_high?: number;
        target_temp_low?: number;
        hvac_mode?: string;
    };
}
export declare class ServiceCommand<I extends keyof ServiceCommandMap> {
    private id;
    private client;
    constructor(id: I, client: Client);
    call(fields: ServiceCommandMap[I]): Promise<void>;
}
