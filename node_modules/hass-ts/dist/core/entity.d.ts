import { Client } from "./client";
import { State } from "../types";
export declare class Entity<I extends string> {
    private id;
    private client;
    private _state;
    private stateLoadedListener;
    constructor(id: I, client: Client);
    private onStateLoaded;
    onStateChanged(callback: (oldState: State, newState: State) => void): void;
    get state(): State;
    stateListener(): void;
}
